global:
  resolve_timeout: 5m

route:
  receiver: discord
  group_by: ["alertname", "service"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2m

receivers:
  - name: discord
    webhook_configs:
      - url: __DISCORD_WEBHOOK_URL__
        send_resolved: true
